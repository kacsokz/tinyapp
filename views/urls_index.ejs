<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <% include ./partials/_head_bootstrap %>
    <title>Index - TinyApp v.kacsokz</title>
  </head>

  <body>
    <head>
      <% include ./partials/_header %>
    </head>
    <main style="margin: 1em;">
      <h3>My URLs</h3>
      <table class="table">
        <thead>
          <tr>
            <th scope="col">Short URL</th>
            <th scope="col">Long URL</th>
            <th scope="col">Edit</th>
            <th scope="col">Delete</th>
          </tr>
        </thead>
        <tbody>
          <% if (user) { %>
            <% for(let url in userURLs) { %>
              <tr>
                <td><%= url %></td>
                <td><%= urls[url]["longURL"] %></td>
                <td>  
                  <a class="btn btn-outline-primary" href="/urls/<%= url %>/update">Edit</a>
                </td>
                <td>  
                  <form method="POST" action="/urls/<%= url %>/delete">
                    <button class="btn btn-outline-danger">Delete</button>
                  </form>
                </td>
              </tr>
            <% } %>
          <% }; %>
        </tbody>
      </table>
    </main>
    <% include ./partials/_body_bootstrap %>
  </body>
</html>